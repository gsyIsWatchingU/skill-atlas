{
  "name": "skill-atlas",
  "version": "2.0.0",
  "description": "扫描、存储、比较并跨设备安装 Agent Skills 的在线应用",
  "private": true,
  "license": "MIT",
  "scripts": {
    "start": "node src/web-server.js",
    "dev": "node --watch src/web-server.js",
    "test": "node --test test/web-server.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
